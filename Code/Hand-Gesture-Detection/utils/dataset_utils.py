import pandas as pd


dataset_path = "data/"

def add_example_to_dataset(label:str, hand_side: str, data: list[list[list[int]]]):
    """
    input: 
        label - rock/paper/scissors
        hand_side - left/right
        data - a list of all ladmarks, each landmark is [[x0, y0, z0], [x1, y1, z1], ...]
    
        return: None
    """
    pass